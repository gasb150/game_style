<% if current_user.nil? %>
<% controller.redirect_to root_path %>
    <% else %>

<div class="new-article cl-lg">
<h1>New Article</h1>

<%= render 'form', article: @article %>

<%= link_to 'Back', articles_path %>
</div>
<% end %>